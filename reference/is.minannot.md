# Check whether object is a minannot instance.

Check whether object is a minannot instance.

## Usage

``` r
is.minannot(x)
```

## Arguments

- x:

  any \`R\` object

## Value

logical, `TRUE` if parameter `x` is a minannot instance (that is, has
"minannot" in its classes) and `FALSE` otherwise.
